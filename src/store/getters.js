const getters = {
  modules: (state) => state.modules,
  lunis: (state) => state.lunis,
  ap: (state) => state.ap,
  ad: (state) => state.ad,
  cost: (state) => state.cost,
  itemTypes: (state) => state.itemTypes,
  CarryAvailable: (state) => state.CarryAvailable
};

export { getters };
